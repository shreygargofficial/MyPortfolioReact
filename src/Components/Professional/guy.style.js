import { StyleSheet } from "aphrodite";

export const GuyStyle = StyleSheet.create({
    g:{
        display:'block',
    },
    svg:{
        width: '100%',
        height: '100%',
        transform: 'translate3d(0, 0, 0)',
        contentVisibility: 'visible'}
})